import styled from "styled-components";

export const Container = styled.section`
 border: 2px dashed rgba(28, 28, 28, 0.5);
  max-width: 1280px;
  margin: 4.4rem auto 2.2rem auto;
  padding: 2rem;
  border-radius: 15px;
  @media (max-width: 820px) {
    margin: 2rem auto 0.8rem auto;
  }
`;
